{% extends 'base.html.twig' %}

{% block title %}Mon Profil - BabsGarage{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row align-items-center">
            <div class="col-md-3 text-center">
                <img src="{{ app.user.picture }}" class="avatar mb-3" alt="Avatar de {{ app.user.firstName }}">
                <h2>{{ app.user.fullName }}</h2>
                <span class="badge bg-primary">Membre</span>
            </div>
            <div class="col-md-9">
                <div class="card p-4 shadow-sm border-0">
                    <h3>Mes informations</h3>
                    <hr>
                    <p><strong>Email :</strong> {{ app.user.email }}</p>
                    <p><strong>Rôle :</strong> {{ app.user.roles[0] }}</p>
                    <a href="{{ path('account_profile') }}" class="btn btn-outline-primary">Modifier mon profil</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
