{% extends 'base.html.twig' %}

{% block title %}Ajouter une voiture{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-primary text-uppercase mb-4">Mettre un véhicule en vente</h1>

        <div class="card shadow-sm border-0 p-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary btn-lg mt-3">Publier l'annonce</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
